{
  "name": "mimo-laundry-os",
  "private": true,
  "packageManager": "pnpm@9.15.5",
  "scripts": {
    "build": "echo Build is not configured in Chapter 1.2",
    "test": "echo Test is not configured in Chapter 1.2",
    "typecheck": "echo Typecheck is not configured in Chapter 1.2",
    "lint": "prettier --check .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.8.1",
    "turbo": "^2.8.14"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,md,yml,yaml}": [
      "prettier --check"
    ]
  }
}
